@model IEnumerable<Uset_zayavok.Models.Request>

@{
    ViewBag.Title = "Список заявок";
}

<div class="container">
    <h2>Учет заявок на ремонт техники</h2>

    <form method="get" class="mb-3">
        <div class="input-group">
            <input type="text" name="search" class="form-control" placeholder="Поиск по номеру или модели..." />
            <button type="submit" class="btn btn-primary">Найти</button>
        </div>
    </form>

    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>№</th>
                <th>Дата</th>
                <th>Техника</th>
                <th>Модель</th>
                <th>Статус</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Requestid</td>
                    <td>@(item.Startdate?.ToShortDateString())</td>
                    <td>@item.Hometechtype</td>
                    <td>@item.Hometechmodel</td>
                    <td>
                        <span class="badge @(item.Requeststatus == "Готова к выдаче" ? "bg-success" : "bg-warning")">
                            @item.Requeststatus
                        </span>
                    </td>
                    <td>
                        @if (User.IsInRole("Мастер") || User.IsInRole("Администратор"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.Requestid" class="btn btn-sm btn-info">Редактировать</a>
                        }
                        else
                        {
                            <span class="text-muted">Только просмотр</span>
                        }
                    </td>
                </tr>
                @if (User.IsInRole("Менеджер"))
                {
                    <div class="alert alert-warning">
                        <strong>Режим менеджера по качеству:</strong> Вы можете изменить описание проблемы
                        для фиксации продления срока или переназначить мастера.
                    </div>
                }
            }
        </tbody>
    </table>
</div>